{
  "name": "kutsenko-dev",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npx serve frontend/dist",
    "serve:simpleprocess": "npx serve sites/simpleprocess -p 8081"
  }
}
